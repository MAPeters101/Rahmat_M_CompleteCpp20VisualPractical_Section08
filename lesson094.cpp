#include <iostream>

using namespace std;

/*
int main() {
	int Array[5]{ 1, 2, 3, 4, 5 };

	int* Arrayptr{ Array };
	int* Newptr{ Arrayptr + 2 };
	// {Array + (2 * sizeof(int)}

	//int* Newptr1{ *Arrayptr + 2 };
	int ArrayValue2{ *Arrayptr + 2 };
	cout << ArrayValue2 << endl;
	ArrayValue2 = (*Arrayptr) + 2;
	cout << ArrayValue2 << endl;

	int ArrayS[5]{ 1, 2, 3, 4, 5 };
	int* ArraySptr{ ArrayS };
	int* ArraySptr2{ ArrayS + 4 };
	cout << "------------------------------------\n\n";

	auto Difference{ ArraySptr2 - ArraySptr };
	cout << Difference << endl;

	int NewArray[5]{ 3, 9, 5, 2, 1 };

	int* NewArrayptr{ NewArray };

	for (int i{ 0 }; i < 5; i++) {
		cout << *NewArrayptr++ << endl;
	}
	cout << "\n\n";

	double NewArray2[5]{ 3.0, 9.6, 5.9, 2.2, 11.6 };

	double* NewArrayptr2{ NewArray2 };

	for (int i{ 0 }; i < 5; i++) {
		cout << *NewArrayptr2++ << endl;
	}
	cout << "\n\n";


	return 0;
}
*/


